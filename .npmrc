# React Native + Metro do not handle pnpm's symlinked node_modules well, and the
# RN Android Gradle scripts hardcode flat `../node_modules/...` paths. A hoisted
# node-linker gives an npm/yarn-style flat layout that both tools expect.
node-linker=hoisted
